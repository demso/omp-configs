# Скопируй в device.env и подстрой под устройство. device.env не коммитится.
# WSL-пользователь агента (дом: /home/$AGENT_USER — подставляется в шаблоны)
AGENT_USER=agent
# Корень данных агентов на Linux-стороне (mcps, DOCS) — подставляется в mcp.json
AGENT_DATA_ROOT=/mnt/d/data/agent
